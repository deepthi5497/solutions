1)select * from emp order by salary desc limit 3;
2)select  emp_name,min(salary) from emp;
3)
4)select * from emp where emp_id in (select man_id from emp);
5)select *from emp where salary=(select min(salary) from emp where emp_id in(select man_id from emp));
6)select count(emp_id) from emp where dept_id in (select dept_id from dept where dept_name="comm");
7)select * from emp where (dept_id,salary) in (select d.dept_id,max(salary) from emp e join dep d on e.dept_id=d.dept_id  where dept_name="finance");
8)select * from emp where (dept_id,salary) in (select d.dept_id,min(salary) from emp e join dep d on e.dept_id=d.dept_id  where dept_name="product");
9)select * from emp where dept_id = (select dept_id from dep where dept_name="Health");
10)select * from emp where man_id = (select emp_id from emp where emp_name="Natasha Stevens");
11)
12)select emp_id,man_id,dept_id from emp where dept_id =(select dept_id from dep where dept_name="comm");
13)select e.dept_id,avg(salary),d.dept_name from emp e join dep d on e.dept_id=d.dept_id group by dept_id;
14)select sum(salary) as total_expenses from emp where dept_id in(select dept_id from dep where dept_name='FINANCE');
15)select d.dept_id,d.dept_manager,count(*) from emp e join dep d on e.dept_id=d.dept_id group by d.dept_id having count(*) =(select min(member) from (select dept_id,count(*) as member from emp group by dept_id) as count_emp);
16)select dept_id,count(*) as count from emp group by dept_id;
17)select dept_id,count(*) as count from emp where salary<10000 group by dept_id;
18)select count(emp_id) as count from emp where dept_id='D04';
19)select e.dept_id,d.dept_name,d.dept_manager,count(*) from emp e join dep d on e.dept_id=d.dept_id group by d.dept_id having count(*) =(select max(member) from (select dept_id,count(*) as member from emp group by dept_id) as count_emp);
20)select emp_id,emp_name from emp where man_id in (select emp_id from emp where emp_name="TIM COOK");
20.1) select emp_id,emp_name from emp where man_id = (select emp_id from emp where emp_name="TIM ARCHER");
